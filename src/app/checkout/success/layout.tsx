import type { Metadata } from 'next'

export const metadata: Metadata = {
    title: 'Upgrade Successful — MyGang',
}

export default function CheckoutSuccessLayout({ children }: { children: React.ReactNode }) {
    return children
}
